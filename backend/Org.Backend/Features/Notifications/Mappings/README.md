# Notifications Mappings

## Notification Entity → NotificationDto
- Map all notification fields
- Include Actor details (if present)
- Convert NotificationType enum to string
- Format timestamps for display

## NOT Implemented in Phase 3C
- ❌ No real mapping implementations
