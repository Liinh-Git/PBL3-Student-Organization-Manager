# Requests Mappings

## Request Entity → RequestDto
- Map all request fields
- Include Sender details (User)
- Include Organization details
- Include DesiredDepartment details (if assigned)
- Include ReviewedByMember details (if reviewed)
- Convert RequestType and RequestStatus enums to strings

## NOT Implemented in Phase 3C
- ❌ No real mapping implementations
