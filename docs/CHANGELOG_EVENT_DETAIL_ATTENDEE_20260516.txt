CHANGELOG - EVENT DETAIL PAGE + ATTENDEE JOIN FLOW
Date: 2026-05-16
Workspace: D:\PBL\PBL3

================================================================
1) GOAL
================================================================
- Build event detail page for viewing full event data when clicking event cards.
- Add join event action for users who have not joined.
- Add preview/edit modes on the same detail page with inline click-to-edit behavior.
- Respect permission logic based on organization membership and event permissions.

================================================================
2) BACKEND CHANGES
================================================================

2.1 New shared attendee contract
- Added file:
  - backend/Org.Shared/Features/Attendees/AttendeeContracts.cs
- Added DTO:
  - AttendeeRegistrationDto
    - EventId
    - UserId
    - IsRegistered
    - AttendeeId
    - Status
    - RegisteredAtUtc
    - CheckedInAtUtc

2.2 New attendee service
- Added files:
  - backend/Org.Backend/Features/Attendees/Services/IAttendeeService.cs
  - backend/Org.Backend/Features/Attendees/Services/AttendeeService.cs
- Implemented:
  - GetMyRegistrationAsync(eventId, userId)
  - RegisterMeAsync(eventId, userId)
- Business rules:
  - Validate event exists.
  - Block joining for Cancelled/Archived/Completed events.
  - Public event: any authenticated user can join.
  - Non-public event: only active org members can join.
  - Re-activate canceled attendee registrations on re-join.

2.3 New attendee endpoints
- Added files:
  - backend/Org.Backend/Features/Attendees/Endpoints/GetMyEventRegistrationEndpoint.cs
  - backend/Org.Backend/Features/Attendees/Endpoints/RegisterMyEventAttendanceEndpoint.cs
- Added routes:
  - GET /api/events/{eventId}/attendees/me
  - POST /api/events/{eventId}/attendees
  - POST /api/events/{eventId}/attendees/me

2.4 Dependency injection
- Updated:
  - backend/Org.Backend/Program.cs
- Added service registration:
  - IAttendeeService -> AttendeeService

================================================================
3) FRONTEND CHANGES
================================================================

3.1 New attendee API client
- Added:
  - frontend/src/services/attendeeService.js
- Added functions:
  - getMyEventRegistration(eventId)
  - joinEvent(eventId)

3.2 New event detail page implementation
- Added:
  - frontend/src/pages/user/EventDetailPage.jsx
  - frontend/src/pages/user/EventDetailPage.css
- Implemented:
  - Long-form detail page for `/events/:eventId`
  - Top-left back button
  - Load event using:
    - workspace endpoint first (if authenticated and authorized): GET /api/events/{id}
    - fallback public endpoint: GET /api/events/{id}/public
  - Show all available event fields (editable + read-only metadata + raw JSON snapshot)
  - Join button with real attendee API integration
  - Inline click-to-edit fields in edit mode (not separate form)
  - Save changes through PUT /api/events/{id}
  - Preview/Edit mode toggle

3.3 Permission behavior on detail page
- Page checks org permissions from:
  - GET /api/organizations/{orgId}/permissions/me
- Edit mode is enabled only when user has BOTH:
  - org.events.create
  - org.events.manage
- Others stay in preview mode.

3.4 Router update
- Updated:
  - frontend/src/router/AppRouter.jsx
- Route `/events/:eventId` now uses:
  - EventDetailPage (new implementation)

3.5 Event card navigation update
- Updated:
  - frontend/src/pages/user/UserDiscoverPage.jsx
  - frontend/src/pages/user/UserEventsPage.jsx
- Behavior:
  - Event detail navigation now goes to `/events/:eventId` for consistent preview/edit page flow.

================================================================
4) BUILD / VALIDATION
================================================================

4.1 Backend build
- Normal `dotnet build` in backend project was blocked by file lock because a running Org.Backend process was holding DLL.
- Verified compilation using alternate output directory:
  - dotnet build -p:OutDir=D:\PBL\PBL3\backend\build-check\Org.Backend\
- Result: Build succeeded (0 errors).

4.2 Frontend build
- Command:
  - cmd /c npm run build
- Result:
  - Vite production build succeeded.

================================================================
5) NOTES
================================================================
- Existing unrelated local changes in OrgRequests files were preserved and not reverted.
- Temporary build-check directory used for backend compilation was removed after verification.
