version: '3.8'
services:
  postgres_db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: org_admin
      POSTGRES_PASSWORD: SecretPassword123!
      POSTGRES_DB: StudentOrgDb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: